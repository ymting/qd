name: ChangeRelease
on:
  workflow_dispatch: {}
  push:
    paths: [CHANGELOG.md]
    branches: [master]
    tags: ["*"]

permissions:
  contents: write

jobs:
  sync:
    if: github.repository == 'qd-today/qd' || github.repository == 'ymting/qd'
    runs-on: ubuntu-latest
    steps:
    - uses: dropseed/changerelease@v1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        # optional
        tag_prefix: ""
        changelog: CHANGELOG.md
        # Fork 只同步自己维护的最近两个版本，避免扫描到未同步的上游旧标签后报错。
        limit: 2
